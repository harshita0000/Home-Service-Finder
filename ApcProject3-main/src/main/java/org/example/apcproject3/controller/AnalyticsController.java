package org.example.apcproject3.controller;

// Temporarily disabled all analytics functionality since MongoDB and MySQL are disabled
/*
import org.example.apcproject3.document.ServiceAnalytics;
import org.example.apcproject3.document.ServiceLog;
import org.example.apcproject3.service.MongoAnalyticsService;
import org.springframework.beans.factory.annotation.Autowired;
import org.springframework.http.ResponseEntity;
import org.springframework.security.access.prepost.PreAuthorize;
import org.springframework.web.bind.annotation.*;

import jakarta.servlet.http.HttpServletRequest;
import java.time.LocalDateTime;
import java.util.HashMap;
import java.util.List;
import java.util.Map;

@RestController
@RequestMapping("/api/analytics")
@CrossOrigin(origins = "*", maxAge = 3600)
public class AnalyticsController {
    // Analytics functionality temporarily disabled - requires MongoDB and MySQL
    // Will be re-enabled when external databases are available
}
*/
